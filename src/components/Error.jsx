import React from "react";

const Error = () => {
  return (
    <section className="error-container">
      <img src="/oops.svg" alt="People with disconnected cable showing error" />
    </section>
  );
};

export default Error;
